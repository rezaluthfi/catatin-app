/// Receivables List Screen — daftar piutang pelanggan.
/// TODO (Sprint 5): Implementasi daftar kasbon + tandai lunas.
import 'package:flutter/material.dart';

class ReceivablesListScreen extends StatelessWidget {
  const ReceivablesListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Piutang Pelanggan')),
      body: const Center(
        child: Text('Piutang — Coming Soon (Sprint 5)'),
      ),
    );
  }
}
