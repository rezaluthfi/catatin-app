/// Receivable Form Screen — form input piutang manual.
/// TODO (Sprint 5): Implementasi form piutang manual.
import 'package:flutter/material.dart';

class ReceivableFormScreen extends StatelessWidget {
  const ReceivableFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tambah Piutang')),
      body: const Center(
        child: Text('Form Piutang Manual — Coming Soon (Sprint 5)'),
      ),
    );
  }
}
